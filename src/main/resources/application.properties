spring.application.name=crud-usuarios

# Puerto del servidor
server.port = 8085

# Configuración de la base de datos
spring.datasource.url = jdbc:mysql://localhost:3306/usuariosdb?useSSL=false&serverTimezone=UTC
spring.datasource.username = root
spring.datasource.password = Pieroandres@3005

# Driver de MySQL
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

# JPA y Hibernate
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# Hola este un mensaje (Probando los conflictos en Git)
# Este es otro mensaje probando los conflictos.
